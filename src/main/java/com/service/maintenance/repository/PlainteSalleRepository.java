package com.service.maintenance.repository;

import com.service.maintenance.model.PlainteSalle;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlainteSalleRepository extends JpaRepository<PlainteSalle, Integer> {
}